#include "lists.h"

/**
 * is_palindome - determine if a singly linked list represents a palindrome
 * @head: a pointer to a pointer to the start of the list
 *
 * Return: 1 if the list represents a palindrome, otherwise 0
 */
int is_palindrome(listint_t **head)
{
	return (0);
}
